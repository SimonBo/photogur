<h1>Photogur</h1>

<%= link_to "New Photo", new_photo_path(@photo)  %>
<h2>Newest Photos</h2>
<% @most_recent_photos.each do |photo|%>
	<%= link_to (image_tag photo.url), photo %>
	<%= photo.title  %>
	<%= photo.author %>
<% end %>

